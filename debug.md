# Debug

## console.dir()

We can see all its properties using `console.dir(element)`, a method provided by browsers to list all the properties of a particular object:

```js
const header = document.createElement('h1');
console.dir(header);
```
